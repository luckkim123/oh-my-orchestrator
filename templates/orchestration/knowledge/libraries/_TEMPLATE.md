# {Library Name}

> Last verified: YYYY-MM-DD against version x.y.z

<!-- Before relying on this file, compare the version above to what is installed.
     Differ -> the file is a lead, not a fact: verify the claim you need and update
     the banner in the same pass. A bumped banner without a re-run check turns an
     old fact into a fresh lie. Stale entries get a banner, never deletion. -->

## Basics

| Field | Value |
|:---|:---|
| Version in use | x.y.z |
| License | |
| Docs | |
| Source | |

## Constraints and gotchas

What bites you. One bullet each, concrete.

## Conflicts with other libraries

- `library-x`: what breaks when both are present.
- `library-y`: version incompatibility, with the versions.

## Performance characteristics

Memory, throughput, and where the cliff is. Numbers, or say the number is unmeasured.

## Patterns in this project

### Recommended

```
```

### Avoid

```
```

Say *why* the avoided pattern is avoided here specifically -- a pattern that is fine
elsewhere and wrong here is the entry worth writing.

## Errors

### `<error text>`

**Cause**:

**Fix**:

## Related files

- `path/to/wrapper` -- where this project wraps the library
- `path/to/test` -- the test that pins the behavior above

---

Section order is fixed. A section with nothing in it says "checked, nothing to
report" -- delete it only if the section does not apply to this library at all.
